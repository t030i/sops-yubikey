# Example secrets file
# Copy this to secrets.yaml and fill in your actual secrets
# Then encrypt with: sops -e secrets.yaml > secrets.enc.yaml

database:
  host: db.example.com
  port: 5432
  username: dbuser
  password: REPLACE_WITH_ACTUAL_PASSWORD

api:
  key: REPLACE_WITH_API_KEY
  secret: REPLACE_WITH_API_SECRET
  endpoint: https://api.example.com

aws:
  access_key_id: AKIAIOSFODNN7EXAMPLE
  secret_access_key: REPLACE_WITH_SECRET_KEY
  region: us-east-1

stripe:
  publishable_key: pk_test_REPLACE
  secret_key: sk_test_REPLACE

# Add your own secrets here
